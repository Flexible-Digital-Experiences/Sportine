spring.application.name=sportine_backend

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/sportine_db
spring.datasource.username=root
spring.datasource.password=n0m3l0

# Configuracin de JPA
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true


# Configuracin del TransactionManager
spring.transaction.default-timeout=30
spring.transaction.rollback-on-commit-failure=true

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Paypal

https://developer.paypal.com/dashboard/)
paypal.client.id=${PAYPAL_CLIENT_ID}
paypal.client.secret=${PAYPAL_CLIENT_SECRET}
paypal.plan.id=${PAYPAL_PLAN_ID}
paypal.mode=sandbox


# URLs de retorno (cambiar cuando tengas tu dominio)
paypal.success-url=sportine://payment/success
paypal.cancel-url=sportine://payment/cancel

# Webhook ID (lo obtendrás después de configurar webhooks)
paypal.webhook-id=PENDIENTE